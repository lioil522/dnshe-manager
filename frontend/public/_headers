# Cloudflare Pages 响应头配置（Vite 会把 public/ 原样拷进 dist/）
#
# Pages 默认给所有文件下发 Cache-Control: public, max-age=0, must-revalidate，
# 于是每次刷新页面，浏览器都要为每个资源发一次条件请求等 304 —— 即使文件名里
# 已经带了内容哈希、内容根本不可能变。首屏三个资源就是三个白等的往返。
#
# Vite 产物中 /assets/ 下的文件名都带内容哈希（如 index-BibuQwET.js），
# 内容一变文件名就变，因此可以安全地设为 immutable 长缓存。

/assets/*
  Cache-Control: public, max-age=31536000, immutable

# index.html 必须保持每次校验，否则新版本发布后用户会一直拿到旧的
# HTML、继续引用已被删除的旧 hash 资源。

/index.html
  Cache-Control: public, max-age=0, must-revalidate
