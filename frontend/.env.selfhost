# 自建（Docker / 裸机）版的前端构建变量
#
# 由 `npm run build:selfhost`（vite build --mode selfhost）加载，Cloudflare Pages
# 走的是默认 production 模式，不会读到本文件。
#
# 前后端在自建版里同源：后端就在同一个端口上把前端发出去。把基准地址设成 "/"，
# App.tsx 里 `storedBackend.replace(/\/$/, "")` 会把它削成空串，请求最终是
# /api/xxx 这样的相对路径 —— 既不用配 CORS，也不用在镜像里烘焙具体域名。
VITE_API_BASE_URL=/
